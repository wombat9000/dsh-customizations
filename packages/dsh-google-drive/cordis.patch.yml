# Session permission service consumes the separately composed Google auth host.
# Installing Drive alone grants no account access; googleAuth must be present.
# Tools are session-toolbar controlled; this bundle never replaces the preset roster.
- insert:
    - id: local-google-drive
      name: '@local/dsh-google-drive'
