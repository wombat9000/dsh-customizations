# Drive UI design screenshots

These screenshots render the actual `client.js` picker component with synthetic file names and the pinned DSH bundle’s native theme CSS. They show the desktop dark/light layouts, the selection review, and the narrow layout. They are PR design evidence, not screenshots of a deployed DSH profile or real Google Drive data.

With the repository's existing pinned Playwright and Chromium available, regenerate them from the repository root:

```sh
node packages/dsh-google-drive/test/capture-picker.mjs
```

The script overwrites the PNGs in this directory. It starts no web server, blocks network requests, and checks modal width, horizontal overflow, fixed-footer behavior, confirmation-button visibility, and primary-button text contrast (at least 4.5:1). It does not update the real-shell visual-test baselines.

- [Initial My Drive view](picker-empty.png)
- [Desktop, dark](picker-dark.png)
- [Desktop, light](picker-light.png)
- [Selection review](picker-selection.png)
- [Narrow layout](picker-mobile.png)
- [Landscape, scrolled selection review](picker-landscape.png)
- Shared with me, with selections retained from My Drive: [Dark](picker-dark-shared.png), [Light](picker-light-shared.png), [Narrow](picker-mobile-shared.png), [Landscape](picker-landscape-shared.png)

Both pickers show location tabs below global search. Root views omit breadcrumbs. The shared-view captures use synthetic shared items and a combined selection count; they do not verify Google sharing permissions.

## Session toggle

These images render the exported `SessionToggle` component beside mock **Session log** and **Recap** controls. The toolbar, session, and status responses are synthetic; the theme CSS comes from the pinned DSH bundle. They do not verify the live header slot or host authorization.

With the existing pinned browser prerequisites, run:

```sh
node packages/dsh-google-drive/test/capture-session-toggle.mjs
```

The script overwrites only `session-toggle-*.png`. It blocks external requests, starts no server, and checks default OFF plus one mocked enable mutation.

- [Dark, OFF](session-toggle-dark.png)
- [Light, OFF](session-toggle-light.png)
- [Narrow, OFF](session-toggle-narrow.png)
- [Dark, enabled](session-toggle-enabled-dark.png)

## Sheets editing and local preview

The Sheets screenshots use the actual edit picker and preview components, synthetic spreadsheet cells, and the same pinned theme. They show supported-format approximation and complete field differences, not Google-calculated results or live Google data.

To regenerate these sixteen PNGs with the existing pinned browser prerequisites:

```sh
node packages/dsh-google-drive/test/capture-sheets.mjs
```

This script overwrites only the `sheets-*.png` files. It starts no server and blocks network requests. It checks modal bounds, horizontal overflow, visible approval controls, and narrow-layout scrolling. Browser interaction tests separately cover 200-cell ranges and complete long-value review.

| View | Dark | Light | Narrow | Landscape |
| --- | --- | --- | --- | --- |
| Local before/after preview | [Dark preview](sheets-preview-dark.png) | [Light preview](sheets-preview-light.png) | [Narrow preview](sheets-preview-narrow.png) | [Landscape preview](sheets-preview-landscape.png) |
| Exact value and formatting differences | [Dark details](sheets-details-dark.png) | [Light details](sheets-details-light.png) | [Narrow details](sheets-details-narrow.png) | [Landscape details](sheets-details-landscape.png) |
| Session edit picker | [Dark picker](sheets-picker-dark.png) | [Light picker](sheets-picker-light.png) | [Narrow picker](sheets-picker-narrow.png) | [Landscape picker](sheets-picker-landscape.png) |
| Shared edit picker, combined selection | [Dark shared picker](sheets-picker-shared-dark.png) | [Light shared picker](sheets-picker-shared-light.png) | [Narrow shared picker](sheets-picker-shared-narrow.png) | [Landscape shared picker](sheets-picker-shared-landscape.png) |

The browser suite also consumes `test/fixtures/sheets-preview.json`, generated by the real Sheets proposal compiler from synthetic responses. `node packages/dsh-google-drive/test/generate-sheets-preview.mjs` regenerates it. The Node suite checks that the committed fixture still matches the compiler; it never rewrites it during validation.
