# Compatibility example for existing user-authored presets; no longer required.
# The host @local/dsh-worktree bundle supplies tools to Standard and other presets.
# This retained row is inert and does not register duplicate definitions.
# Keep the preset's existing tool-jobs row; add it only if absent in a user copy.
- id: local-worktree-tools
  name: '@local/dsh-worktree/tools'

# Required existing job controls:
# - id: tool-jobs
#   name: '@deepseek-ai/dsh-tool-jobs'
